name: Release Rust

on:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

permissions: {}

jobs:
  cliff:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.changelog.outputs.version }}
      changelog: ${{ steps.changelog.outputs.content }}
      release_id: ${{ steps.create-release.outputs.result }}
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/create-github-app-token@v2
        id: app-token
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
      - name: Check out repository
        uses: actions/checkout@v3
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Generate a changelog
        id: changelog
        uses: orhun/git-cliff-action@4a4a951bc43fafe41cd2348d181853f52356bee7 # v4
        with:
          config: cliff.toml
          args: --bump --unreleased
        env:
          OUTPUT: CHANGELOG.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Create release
        id: create-release
        uses: actions/github-script@v7
        env:
          VERSION: ${{ steps.changelog.outputs.version }}
          CHANGELOG: ${{ steps.changelog.outputs.content }}
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          result-encoding: string
          script: |
            const { VERSION, CHANGELOG } = process.env;

            const { owner, repo } = context.repo;

            const release = await github.rest.repos.createRelease({
              owner,
              repo,
              tag_name: VERSION,
              name: VERSION,
              body: CHANGELOG,
            });

            return release.data.id;

      - name: Create Full Changelog
        uses: orhun/git-cliff-action@4a4a951bc43fafe41cd2348d181853f52356bee7 # v4
        with:
          config: cliff.toml
          args: --bump
        env:
          OUTPUT: docs/CHANGELOG.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Commit Changelog
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |-
          git config user.email "cqrl-release-bot@users.noreply.github.com"
          git config user.name "cqrl-release-bot"
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}
          git add docs/CHANGELOG.md
          git commit -m "update changelog"
          git push

  build:
    name: Build - ${{ matrix.platform.os-name }}
    runs-on: ${{ matrix.platform.runs-on }}
    needs:
      - cliff
    strategy:
      matrix:
        platform:
          - os-name: Linux x64
            runs-on: ubuntu-latest
            target: x86_64-unknown-linux-gnu
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/create-github-app-token@v2
        id: app-token
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
      - name: Check out repository
        uses: actions/checkout@v3
        with:
          persist-credentials: false
      - name: Setup Rust
        uses: actions-rust-lang/setup-rust-toolchain@9d7e65c320fdb52dcd45ffaa68deb6c02c8754d9 # v1
        with:
          toolchain: stable
          cache-key: ${{ hashFiles('**/Cargo.lock') }}
          target: ${{ matrix.platform.target }}
      - name: Build binary
        run: |
          cargo build --release --target ${{ matrix.platform.target }}

      - name: Upload binary
        uses: actions/github-script@v7
        env:
          BINARY: target/${{ matrix.platform.target }}/release/cqrl
          RELEASE_ID: ${{ needs.cliff.outputs.version }}
          UPLOAD_ID: ${{ needs.cliff.outputs.release_id }}
          TARGET: ${{ matrix.platform.target }}
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const fs = require('fs');
            const { BINARY, RELEASE_ID, TARGET, UPLOAD_ID } = process.env;

            const { owner, repo } = context.repo;

            await github.rest.repos.uploadReleaseAsset({
              owner,
              repo,
              release_id: UPLOAD_ID,
              name: `cqrl-${RELEASE_ID}-${TARGET}`,
              data: fs.readFileSync(BINARY),
            });
