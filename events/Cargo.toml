[package]
name = "events"
version = "0.1.0"
edition = "2021"

[dependencies]
cloudevents-sdk = { version = "0.8.0", features = ["nats", "nats-lib"] }
errors = { version = "0.1.0", path = "../errors" }
serde = "1.0.218"
serde_json = "1.0.139"
